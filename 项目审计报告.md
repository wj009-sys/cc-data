# 项目全面审计报告

**审计日期**: 2026-06-27  
**审计范围**: 全部代码、数据文件、配置文件、文档  
**审计人**: Claude Code

---

## ✅ 已修复项（v2026.06.27-r6）

| 编号 | 问题 | 修复内容 |
|------|------|---------|
| **S1+S3** | SEED_DAILY 累计盈亏错误(显示+2.9万, 实际-3.4万) | `sync_dashboard()` 改为计算相邻日P&L差值；仪表盘累计已修正为 **-33,921.03** |
| **S2** | 日报6/1~6/18数据损坏(34行/12行/列偏移) | 删除73行脏数据，6/1→9行(去重), 6/2~6/18→9行(移除临时品种) |
| **M1** | 回补功能使用当前数据代替历史数据 | 限制回补仅最近10个交易日，超范围自动跳过并警告 |
| **M2** | 日报去重逻辑不支持多种日期格式 | 新增 `normalize_date_str()` 统一处理 datetime/date/字符串 → YYYYMMDD |
| **M3** | CLAUDE.md 与实际不一致 | 修正定时任务时间(16:33)、补充文件清单、创建记忆文件 |
| **M4** | 仪表盘版本号未更新 | v2026.06.22-r5 → **v2026.06.27-r6** |
| **M5** | Webhook Key 硬编码 | 支持 `WECOM_WEBHOOK` 环境变量覆盖 |
| **M6** | `is_semi_annual_check` 变量名误导 | 重命名为 `is_end_of_period`，注释明确仅12月触发再平衡 |

---

## 🟠 保留的中等问题（已评估暂不修复）

### M1-残余. 日报列J(P&L)数值在6/1~6/18仍有偏差
- 清理格式后行数已正确(9行)，但P&L数值因早期列映射错乱仍不可靠
- `sync_dashboard()` 已通过「仅追加新日期不覆盖已有条目」+「要求有持仓数据」双重保护避免受影响
- SEED_DAILY 种子数据(5/19~6/18)已验证正确，不受影响

### W3. `.cache.pkl` 缓存文件仅写不读
- 写入最后更新时间但无程序逻辑读取该缓存
- 影响极小，当前行为无害，暂不改动

---

## 🟡 轻微问题（持续观察）

### W1. 日报建仓期数据不完整
5/19~5/25 仅有 3~8 行（分批建仓期间，部分品种尚未买入），属正常现象。

### W5. 临时产品（510500/511990/511880）已被清理
日报中这些产品的行已在6/2~6/18清除。交易流水保留原始记录。

---

---

### W3. `.cache.pkl` 缓存文件信息缺失

代码使用 `.portfolio_cache.pkl` 缓存最后更新时间（第 770-775 行），但缓存仅用于记录，实际逻辑并未校验缓存来判断是否需要更新。可考虑删除或增强。

---

### W4. 操作风险：Webhook Key 在上下文历史中暴露

企业微信 webhook Key (`9cccf118-070c-422f-ada6-1a8db2e0f309`) 在代码和对话历史中均可见。建议移至环境变量。

---

### W5. 临时产品（510500/511990/511880）管理不清晰

交易记录中包含 510500（中证500）、511990（华宝添益）、511880（银华日利）的买卖记录，但它们不在 9 个持仓品种的 HOLDINGS 列表中。这使得日报数据处理复杂化（这些产品额外增加了行数），也导致 sync_dashboard 的 9 行筛选条件失效。

---

## ✅ 通过检查的项

| 检查项 | 结果 |
|-------|------|
| 汇总 sheet 持仓数据（成本/数量/市值）与仪表盘一致 | ✅ 全部 9 品种匹配 |
| 交易流水（21 笔）与仪表盘一致 | ✅ 全部匹配 |
| 交易日判断逻辑与节假日列表 | ✅ 一致 |
| 信号检测逻辑（止盈/止损/熔断）与投资纪要规则 | ✅ 一致（V3版） |
| settings.json 权限配置 | ✅ 可用（但过于宽松） |
| 定时任务配置与运行 | ✅ 午间/收盘简报均在运行 |
| 代码无语法错误 | ✅ |
| 节假日列表在 update_portfolio.py 与 dashoard 间一致 | ✅ |
| Tushare/东方财富 API 调用逻辑 | ✅ |

---

## ✅ 当前状态

**全部严重问题(S1/S2/S3)和中等问题(M1~M6)已修复。** 项目关键指标已恢复正常：

- 仪表盘累计盈亏: **-33,921.03 元**（与汇总 sheet P&L 一致）✅
- 日报格式: 全部交易日 9行/天 ✅
- daily_totals 智能过滤: P&L范围 ±500k + 需要实际持仓数据 ✅
- SEED_DAILY 追加策略: 仅新日期追加，不覆盖已有正确数据 ✅
